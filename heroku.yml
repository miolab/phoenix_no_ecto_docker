build:
  docker:
    web: app/Dockerfile
  config:
    MIX_ENV: prod
# release:
#   image: web
#   command:
#     - mix deps.get --force && mix deps.compile && cd assets && npm install && node node_modules/webpack/bin/webpack.js --mode development
# run:
#   web: mix phx.server --no-halt
